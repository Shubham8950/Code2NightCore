@model Code2Night.DAL.Common.MyBlogs
<div class="tags-cloud">
    @if (Model.Tags != null)
    {
        foreach (var item in Model.Tags.Take(20))
        {
        <div class="tag">
            <a href="~/Blog/Tags/@item">@item.Replace("-", " ")</a>
        </div>

        }
    }
</div>
 